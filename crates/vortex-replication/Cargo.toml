[package]
name = "vortex-replication"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "VortexDB leader-follower replication and backlog"

[dependencies]
vortex-common = { workspace = true }
